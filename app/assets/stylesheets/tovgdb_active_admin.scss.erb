<% if Rails.env.development? %>

  body {
    padding-bottom: 144px; ; /* for web-console */
  }

<% end %>

.active_admin .errors {
  list-style: none;
}

.active_admin .errors ul {
  list-style: disc;
}
